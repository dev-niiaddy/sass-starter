# **Sass Starter**

Quick start sass project template with css grid and intuitive project structure.

Initialize project `npm install`

Compile **with** watcher `npm run compilesass` 

Compile **without** watcher `npm run compileonce`
